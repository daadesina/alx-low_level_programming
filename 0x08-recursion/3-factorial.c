#include "main.h"

/**
 * factorial - Entry point
 * @n: an integer
 * Return: the factorial calculation
 */
int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if (n == 0)
	{
		return (1);
	}
	else
	{
		return (n * factorial(n - 1));
	}
}
