#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char my_word[] = "\"Programming is like building a multilingual puzzle";

	puts(my_word);
	return (0);
}
