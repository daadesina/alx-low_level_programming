#include "main.h"

/**
 * print_list - print the list
 * @list_t: a constant
 * @h a constant variable
 * Retrun: nober of nodes
 */
size_t print_list(const list_t *h)
{
	if (str == NULL)
		printf("[0] (nil)");
	else
	{
		return (len);
	}
}
