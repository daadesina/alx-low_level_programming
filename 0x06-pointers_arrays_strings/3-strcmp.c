#include "main.h"
#include <string.h>

/**
 * _strcmp - Entry point
 * @s1: a character
 * @s2: another character
 * Return: the result
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
