#include "main.h"

/**
 * reverse_array - Entry point
 * @a: an integer
 * @n: another integer
 * Return: Nothing to return
 */
void reverse_array(int *a, int n)
{
	int i = 0;
	int j = 0;

	while (i != n)
	{
		i++;
	}
	while (j < i)
	{
		a[j];
		j--;
	}
}
