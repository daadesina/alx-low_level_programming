#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument count
 * @argv: argument vector
 * Return: Always 0
 */
int __attribute__ ((unused))main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
