#include <stdio.h>

/**
 * main - entry mode
 *
 * Return: Always 0
 */
int main(void)
{
	int i;

	printf("nfinite loop incoming :(\n");

	/**
	 * i = 0;
	 *
	 * while (i < 10)
	 * {
	 * 	putchar(i);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\n");

	return (0);
}
